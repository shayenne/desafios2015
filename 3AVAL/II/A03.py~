import sys

n = int(raw_input())
a = map(int, raw_input().split())


for i in xrange(len(a)):
    if i not in dic:
        dic[i] = 0
    dic[i] += 1

for v in dic:
    if dic[v] >= (n/2) + 1:
        print "NO"
        exit

print "YES"
